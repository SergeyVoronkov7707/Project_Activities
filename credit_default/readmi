# Fit_2
* Чтение данных
* Выявление категориальных и числовых признаков
* base line
* анализ распределение train, test используя метрики skew или kurtosis
* Анализ распределения признаков, также анализ распределения логарифмированый признаков
* Анализ признаков train, test смотрю на нулевую гипотезу
* обучение нескольких моделей и на основе предсказаний(lgbm, cb, xgb), нахождение оптимального 
порога если брать предсказания трех моделей
* беру вероятностные предсказания(так называемые Out-of-fold) добавляю полезные признаки, обучаюсь на таких данных
* создаю новые признаки на основе данных, используя множественные преобразования
* делю данные на 10 частей и обучаюсь (KFold(n_splits=10))
# bayes_param
* поиск лучших гиперпараметров на основе "Байесовской оптимизации"
# oof_fit
* обучение на данных с вероятностными предсказаниями
